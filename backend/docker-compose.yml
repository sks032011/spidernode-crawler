version: '3.8'

services:
  redis:
    image: redis:alpine
    container_name: spidernode-redis
    ports:
      - "6379:6379"
    restart: unless-stopped
    volumes:
      - redis-data:/data
    command: redis-server --save 60 1 --loglevel warning

volumes:
  redis-data: